<?php

namespace {{ namespace }};

use Closure;
use Illuminate\Http\Request;

class {{ class }}
{
    public function handle(Request $request, Closure $next): mixed
    {
        return $next($request);
    }
}
